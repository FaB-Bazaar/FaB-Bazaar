// Limits for a user's personal OAuth clients (one per app on /mcp-integration).
// Shared by the create route and the page so both enforce the same numbers.
export const MAX_CLIENTS_PER_USER = 10;
export const MAX_CLIENT_NAME_LENGTH = 100;
