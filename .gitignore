# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# macOS
.DS_Store
**/.DS_Store

# backup files
*.bak

# Python bytecode (pipeline scripts)
**/__pycache__/
*.pyc

# Playwright
# e2e/ is local scratch (auth.json holds a real session, plus screenshots and
# throwaway .mjs probes) — ignored by default. e2e/regression/ is the curated
# exception: committed specs that guard shipped behaviour. Add a spec there
# only if it has no secrets and no machine-specific paths.
e2e/*
!e2e/regression/
# Shared fixtures the regression specs import. No secrets, no machine paths —
# they drive the app through its own UI and API. Kept here rather than copied
# into regression/ because 21 local specs import the same file.
!e2e/helpers/
/test-results/
/playwright-report/

# local-only docs (not for public repo)
DEPLOYMENT.md
claude_auth.md
oauth-testing.md
scripts/db-permissions.sql

# uncompressed seed dumps (commit the .gz, not the raw SQL)
seeds/*.sql

# manual test scripts (local dev only, not for CI)
**/*.manual-test.ts

# local-only Docker Compose overrides (port mappings, dev-only services)
docker-compose.override.yml

# explicitly allow .env.example (needed for Docker build)
!.env.example

# dependencies
/node_modules
node_modules/

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
# Ignore local and base environment files that contain secrets.
.env*
.env.local
.env.production
.env.test
.env
.env.development.local
.env.production.local

# NOTE: .env.example and .env.test are intentionally NOT ignored.
# They are necessary for team collaboration and Vercel builds.

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Cursor IDE
.cursor/
.cursorrules
.cursorrules.json

# Claude Code
.claude/

# Backup logs
logs/

# TLS certificates (private keys must never be committed)
certs/

# Google service-account keys — never commit these
*service-account*.json
ga-sa*.json

# Pipeline — generated data files (runtime artifacts, not source)
pipeline/scripts/cards.enhanced.json
pipeline/scripts/cards.enhanced.report.json
pipeline/scripts/cards.enhanced.summary.txt
pipeline/scripts/cards_with_prices.json
pipeline/scripts/cards_with_prices.price_report.json
pipeline/scripts/cards_with_prices.price_summary.txt
pipeline/scripts/printings_collection_seed.json
pipeline/scripts/price_snapshot.json
pipeline/scripts/market_analysis_export.json
pipeline/scripts/price_history/
pipeline/scripts/logs/post-tournament-decklists.zip

# Local scratch notes (draft article content, throwaway scratchpads).
# Root-anchored so it ONLY ignores a top-level notes/ dir — an unanchored
# "notes/" also matched nested app routes like app/api/decks/[deckId]/notes/,
# silently dropping that route from git (and therefore from prod builds).
/notes/
# Root-level throwaway scripts (scratch-iar-dryrun.ts and the like) — never
# meant for the public repo; real tooling lives in scripts/.
/scratch-*

# Database dumps / backups (pg_dump artifacts — never commit)
*.dump
*.sql.gz
prod_full_*.sql
printings_backup_*.sql

# One-off i18n prod backfill data export (large CSV; SQL runbook kept)
scripts/i18n-prod-backfill/*.csv

# Curated facet seed scripts are internal-only (repo is public)
scripts/seed-*-facets.ts

# Generated facet-tag export — attribution lives in the DB, never committed
# (git history can't honor a later retraction). Regenerate locally if needed.
card-facets.json
